Exit code: 0
Wall time: 1.2 seconds
Output:
# BLE Protocol v1

The custom service (`12345678-1234-5678-1234-56789abcdef0`) has three vendor-specific characteristics:

| UUID suffix | Properties | Purpose |
| --- | --- | --- |
| `鈥bcdef1` | Write | Command request frames |
| `鈥bcdef2` | Notify | Responses and asynchronous events |
| `鈥bcdef3` | Write / Write Without Response | OTA firmware chunks |

Every command/event frame is little-endian:

`AA 55 | version:1 | flags:1 | command:1 | sequence:2 | payload_length:2 | payload:N | CRC16-Modbus:2`

`sequence` is copied into ACK/error replies. Notifications generated by the device use sequence zero. GATT writes and notifications may contain partial or multiple frames; both ends must buffer and decode by length. The protocol CRC complements the BLE link CRC and keeps this framing reusable on other transports.

`SET_WIFI (0x01)` payload is `ssid_length:1 | password_length:1 | ssid | password`. `OTA_BEGIN (0x60)` has a 32-bit firmware length. OTA bytes are deliberately not framed as ordinary commands; they use the dedicated data characteristic.

